# 👑 Little Princesses ERP — تقرير التدقيق والإنجاز الشامل لنظام تعدد العملات والربط المحاسبي الكامل

تم بنجاح فحص واستكمال وضبط منظومة **تعدد العملات والربط المحاسبي الكامل** لنظام **Little Princesses ERP** مع جعل **الريال اليمني (YER)** العملة الأساسية والوظيفية للنظام (Base / Functional Currency)، وإزالة أي افتراض سابق للدولار الأمريكي `USD`، مع الحفاظ التام على دعم العملات الثلاث (`YER` / `SAR` / `USD`)، وتطبيق سياسة منع التكرار (Zero Duplicates).

---

## 📊 جدول الحالات الإجباري للـ 24 عنصراً المحاسبياً والبرمجياً

| # | العنصر المحاسبي / البرمجي | الحالة | التوضيح الفعلي في النظام |
| :---: | :--- | :---: | :--- |
| 1 | **YER Base Currency** | ✅ تم | تم اعتماد الريال اليمني كعملة أساسية ووظيفية موحدة (Rate = 1.0) وإصلاح جميع نقاط البداية الافتراضية. |
| 2 | **SAR (الريال السعودي)** | ✅ تم | مدعوم بالكامل في التحويلات، وتثبيت السعر، والقيود، والسندات، والإعدادات. |
| 3 | **USD (الدولار الأمريكي)** | ✅ تم | مدعوم كعملة إضافية مع إمكانية تحويله وتثبيت سعر صرفه، دون أن يكون العملة الأساسية. |
| 4 | **CurrencyService** | ✅ تم | خدمة مركزية خفيفة موحدة لإدارة العملات، والتحويل، والتنسيق، وحساب الفروق. |
| 5 | **أسعار الصرف (Exchange Rates)** | ✅ تم | قابلة للتعديل والإدارة من شاشة الإعدادات وREST API ومرتبطة بقاعدة البيانات وGoogle Sheets. |
| 6 | **سعر الصرف وقت العملية** | ✅ تم | تثبيت السعر لحظياً في كل عملية (Lock-in) وحفظ القيمة الأصلية والمكافئ بالـ YER التاريخي. |
| 7 | **شجرة الحسابات (Chart of Accounts)** | ✅ تم | الحفاظ الكامل على الحسابات وأكوادها، وإصلاح عرض الرصيد من `0 USD $` إلى `0 YER ﷼`. |
| 8 | **القيود اليومية (Journal Entries)** | ✅ تم | قيد مزدوج متوازن على أساس الريال اليمني ($\sum \text{Debit YER} = \sum \text{Credit YER}$). |
| 9 | **دفتر الأستاذ العام (General Ledger)** | ✅ تم | مشتق آلياً ولحظياً من القيود اليومية دون أي إدخال يدوي مكرر مع حساب الرصيد التراكمي. |
| 10 | **ميزان المراجعة (Trial Balance)** | ✅ تم | مولد آلياً بالريال اليمني بالمجاميع والأرصدة مع التحقق الصارم من التوازن. |
| 11 | **فروق العملات (Gain / Loss)** | ✅ تم | ربط حسابي `402 أرباح فروق الصرف` و`506 خسائر فروق الصرف` مع محرك الفروق المحاسبية. |
| 12 | **الخزينة (Treasury)** | ✅ تم | ربط حسابات الصناديق بالعملات (`101` YER, `101.2` SAR, `101.3` USD). |
| 13 | **البنوك (Banks)** | ✅ تم | ربط حسابات البنوك بالعملات (`103` YER, `103.2` SAR, `103.3` USD). |
| 14 | **العملاء (Customers)** | ✅ تم | توليد القيود الآلية لمبيعات وعربونات العملاء مع تثبيت سعر الصرف ومعادل `base_amount`. |
| 15 | **الموردون (Suppliers)** | ✅ تم | ربط فواتير وسندات الموردين بالحسابات المزدوجة وسعر الصرف اللحظي. |
| 16 | **المبيعات والطلبات (Orders)** | ✅ تم | اختيار العملة وتثبيت سعر الصرف وترحيل قيود إيرادات المبيعات وذمم العملاء. |
| 17 | **المشتريات (Purchases)** | ✅ تم | دعم العملات الثلاث، توليد سند صرف آلي للفوري، وترحيل آجل للموردين مع $0$ أثر على الصندوق. |
| 18 | **المصروفات (Expenses)** | ✅ تم | تسجيل العملة وسعر الصرف وتوليد قيود المصروفات وحفظ القيمة بالريال اليمني. |
| 19 | **السندات المالية (Vouchers)** | ✅ تم | سندات قبض وصرف مع Transaction ID، وتثبيت سعر الصرف والمكافئ بـ YER. |
| 20 | **الرواتب (HR & Payroll)** | ✅ تم | توليد قيود صرف الرواتب والسلف بحسابات قياسية (`501` و `101` و `104`) ومكافئ YER. |
| 21 | **Google Sheets Backend** | ✅ تم | تحديث `MASTER_SCHEMA_MAP` بأعمدة `exchange_rate` و `base_amount` لكافة الجداول. |
| 22 | **Google Apps Script** | ✅ تم | دعم قراءة وكتابة وتحديث حقول العملات وأسعار الصرف بأمان. |
| 23 | **Transaction ID** | ✅ تم | توليد معرف عملية فريد لكل حركة مالية وقيد وسند. |
| 24 | **منع التكرار (Idempotency)** | ✅ تم | تطبيق `appendOrUpdateRow` لمطابقة الـ ID وتحديث السجل عند الإعادة لمنع التكرار (Zero Duplicates). |

---

## 🧪 نتائج الاختبارات الإلزامية الستة (Verification Results)

1. **اختبار YER:** تم تسجيل عملية بـ `100 YER` وحفظها مباشرة كعملة أساسية بمعامل $1.0$.
2. **اختبار SAR:** تم تسجيل حركة بـ `100 SAR`، وثُبت سعر الصرف $142.5$، وحُسب المعادل $14,250 \text{ YER}$ وتولدت القيود ودخلت دفتر الأستاذ وميزان المراجعة.
3. **اختبار USD:** تم تسجيل حركة بـ `100 USD`، وحُسب المعادل $53,600 \text{ YER}$ بنجاح.
4. **اختبار تغير سعر الصرف:** عند تعديل سعر الصرف، احتفظت المعاملات التاريخية القديمة بأسعار صرفها ومكافئها بالريال اليمني دون أي تأثر.
5. **اختبار التكرار (Idempotency):** تم إرسال نفس المعاملة مرتين عبر `POST /api/gas` وتأكد النظام من تحديث السجل القائم بدلاً من إنشاء سجل مكرر.
6. **اختبار شجرة الحسابات:** أصبحت تعرض `0 YER ﷼` بدلاً من `0 USD $` عند الرصيد الافتراضي مع دعم عرض العملة الأصلية عند وجود حركات.

---

## 8. 🚚 ترحيل ونقل البيانات السابقة من Google Sheets إلى PostgreSQL (Data Migration & Seeding)

تم بنجاح بناء وتشغيل سكريبت الترحيل المركزي المستقل [`migrate_sheets_data.py`](file:///c:/Users/Mohammed%20Falah/Little_Princesses_ERP/migrate_sheets_data.py) لقراءة ملفات التصدير السابقة (CSV)، ومعالجة وتوافق البيانات (Data Mapping)، وإدخالها داخل جداول PostgreSQL السحابية وفق الترتيب الصارم للعلاقات وبشكل آمن تماماً ضد التكرار (Idempotent).

### أ. ملفات المصدر وترتيب الترحيل المعتمد:
1. **`10_Company_Profile.csv`** -> جدول `company_profile`: اسم المنشأة، الهاتف، العنوان، والبريد.
2. **`11_Users.csv`** -> جدول `users`: تشفير كلمات المرور باستخدام الملح المعتمد (`little_princesses_erp_salt_2026`) وترجمة الأدوار القياسية.
3. **`7_Accounts.csv`** -> جدول `chart_of_accounts`: رموز الحسابات المالية (101، 102، 104، 105، 201، 301، 401، 501...) والأرصدة بالريال اليمني.
4. **`1_Customers.csv`** -> جدول `customers`: بيانات العميل، أرقام الهواتف، منصة التواصل (واتساب، انستغرام، تيك توك)، والعناوين.
5. **`2_Customer_Measurements.csv`** -> جدولي `children` و `measurements`: ربط القياسات بالعملاء والأطفال مع أطوال الفساتين ومحيط الصدر والخصر والأكمام.
6. **`4_Inventory_Items.csv`** -> جدول `inventory`: الخامات، الأقمشة (حرير ياباني، مخمل، دانتيل، شيفون، كريب، خيوط، سحابات)، الكميات، وتكلفة الوحدة.
7. **`3_Models_Products.csv`** -> جدول `products`: الموديلات، التصنيفات، أسعار البيع، التكلفة، وتفاصيل استهلاك الأقمشة.
8. **`5_Purchases.csv`** -> جدولي `suppliers` و `purchases`: تأسيس الموردين أولاً لتفادي أخطاء المفاتيح الأجنبية، ثم ترحيل فواتير المشتريات بأسعار الصرف وإجمالي الريال اليمني.
9. **`6_Orders.csv`** -> جدولي `orders` و `order_items`: فواتير المبيعات، العرابين المدفوعة، المبالغ المتبقية، وتواريخ التسليم ومراحل المعمل.
10. **`8_Vouchers.csv`** -> جدول `payments`: سندات القبض والصرف وربطها بالعملاء والموردين.
11. **`9_Journal_Entries.csv`** -> جدولي `journal_entries` و `journal_entry_lines`: قيود اليومية المحاسبية العامة مع بناء سطور القيد المزدوج المتوازن (مدين ودائن).

### ب. نتائج تنفيذ الترحيل الفعلي:
```text
===========================================================================
👑 LITTLE PRINCESSES ERP — ترحيل بيانات Google Sheets إلى PostgreSQL
===========================================================================
✅ [1/11] تم ترحيل بيانات الشركة: 1 سجل.
✅ [2/11] تم ترحيل المستخدمين: 3 مستخدمين.
✅ [3/11] تم ترحيل شجرة الحسابات: 12 حساباً.
✅ [4/11] تم ترحيل العملاء: 5 عميلاً.
✅ [5/11] تم ترحيل بروفايلات القياسات والأطفال: 5 قياساً.
✅ [6/11] تم ترحيل أصناف المخزون: 7 أصناف.
✅ [7/11] تم ترحيل الموديلات والمنتجات: 5 منتجات.
✅ [8/11] تم ترحيل فواتير المشتريات والموردين: 3 فواتير.
✅ [9/11] تم ترحيل الطلبات والمبيعات: 5 طلبات.
✅ [10/11] تم ترحيل السندات المالية: 5 سندات.
✅ [11/11] تم ترحيل قيود اليومية العامة المزدوجة: 4 قيود.

===========================================================================
🎉 اكتمل ترحيل كافة بيانات Google Sheets إلى PostgreSQL بنجاح تام!
===========================================================================
```

### ج. التحقق الميداني الحي من واجهات الخادم (Live API Verification):
تم فحص الاستجابات الحية من خادم التطبيق النشط على المنفذ `5000`، وجاءت جميع البيانات المرحلة متاحة ومتطابقة تماماً:
* `getCustomers`        : 6 سجلات (العملاء المرحلون + العميل العام).
* `getOrders`           : 5 طلبات بمجموع مبيعات `471,700 YER`.
* `getProducts`         : 5 موديلات فساتين جاهزة.
* `getInventory`        : 7 أصناف أقمشة ومستلزمات خياطة.
* `getPurchases`        : 3 فواتير مشتريات خامات.
* `getVouchers`         : 5 سندات مالية (قبض وصرف).
* `getJournalEntries`   : 4 قيود محاسبية مزدوجة مرحلة.
* **اختبار التكرار (Idempotency Test):** تم إعادة تشغيل السكربت ولم يتكرر أي سجل، مما يضمن أمان التشغيل في أي وقت.

---

## 🐘 المرحلة الثانية: البنية التحتية والمخطط العلائقي الشامل لقاعدة بيانات PostgreSQL

*جميع الاختبارات نُفذت بنجاح كامل وخرجت بالكود `0`، مع إعادة تصفير كافة الحركات التشغيلية والبدء بقاعدة بيانات نقية 100%.*

---

## 8. 🚚 ترحيل ونقل البيانات السابقة من Google Sheets إلى PostgreSQL (Data Migration & Seeding)

تم بنجاح بناء وتشغيل سكريبت الترحيل المركزي المستقل [`migrate_sheets_data.py`](file:///c:/Users/Mohammed%20Falah/Little_Princesses_ERP/migrate_sheets_data.py) لقراءة ملفات التصدير السابقة (CSV)، ومعالجة وتوافق البيانات (Data Mapping)، وإدخالها داخل جداول PostgreSQL السحابية وفق الترتيب الصارم للعلاقات وبشكل آمن تماماً ضد التكرار (Idempotent).

### أ. ملفات المصدر وترتيب الترحيل المعتمد:
1. **`10_Company_Profile.csv`** -> جدول `company_profile`: اسم المنشأة، الهاتف، العنوان، والبريد.
2. **`11_Users.csv`** -> جدول `users`: تشفير كلمات المرور باستخدام الملح المعتمد (`little_princesses_erp_salt_2026`) وترجمة الأدوار القياسية.
3. **`7_Accounts.csv`** -> جدول `chart_of_accounts`: رموز الحسابات المالية (101، 102، 104، 105، 201، 301، 401، 501...) والأرصدة بالريال اليمني.
4. **`1_Customers.csv`** -> جدول `customers`: بيانات العميل، أرقام الهواتف، منصة التواصل (واتساب، انستغرام، تيك توك)، والعناوين.
5. **`2_Customer_Measurements.csv`** -> جدولي `children` و `measurements`: ربط القياسات بالعملاء والأطفال مع أطوال الفساتين ومحيط الصدر والخصر والأكمام.
6. **`4_Inventory_Items.csv`** -> جدول `inventory`: الخامات، الأقمشة (حرير ياباني، مخمل، دانتيل، شيفون، كريب، خيوط، سحابات)، الكميات، وتكلفة الوحدة.
7. **`3_Models_Products.csv`** -> جدول `products`: الموديلات، التصنيفات، أسعار البيع، التكلفة، وتفاصيل استهلاك الأقمشة.
8. **`5_Purchases.csv`** -> جدولي `suppliers` و `purchases`: تأسيس الموردين أولاً لتفادي أخطاء المفاتيح الأجنبية، ثم ترحيل فواتير المشتريات بأسعار الصرف وإجمالي الريال اليمني.
9. **`6_Orders.csv`** -> جدولي `orders` و `order_items`: فواتير المبيعات، العرابين المدفوعة، المبالغ المتبقية، وتواريخ التسليم ومراحل المعمل.
10. **`8_Vouchers.csv`** -> جدول `payments`: سندات القبض والصرف وربطها بالعملاء والموردين.
11. **`9_Journal_Entries.csv`** -> جدولي `journal_entries` و `journal_entry_lines`: قيود اليومية المحاسبية العامة مع بناء سطور القيد المزدوج المتوازن (مدين ودائن).

### ب. نتائج تنفيذ الترحيل الفعلي:
```text
===========================================================================
👑 LITTLE PRINCESSES ERP — ترحيل بيانات Google Sheets إلى PostgreSQL
===========================================================================
✅ [1/11] تم ترحيل بيانات الشركة: 1 سجل.
✅ [2/11] تم ترحيل المستخدمين: 3 مستخدمين.
✅ [3/11] تم ترحيل شجرة الحسابات: 12 حساباً.
✅ [4/11] تم ترحيل العملاء: 5 عميلاً.
✅ [5/11] تم ترحيل بروفايلات القياسات والأطفال: 5 قياساً.
✅ [6/11] تم ترحيل أصناف المخزون: 7 أصناف.
✅ [7/11] تم ترحيل الموديلات والمنتجات: 5 منتجات.
✅ [8/11] تم ترحيل فواتير المشتريات والموردين: 3 فواتير.
✅ [9/11] تم ترحيل الطلبات والمبيعات: 5 طلبات.
✅ [10/11] تم ترحيل السندات المالية: 5 سندات.
✅ [11/11] تم ترحيل قيود اليومية العامة المزدوجة: 4 قيود.

===========================================================================
🎉 اكتمل ترحيل كافة بيانات Google Sheets إلى PostgreSQL بنجاح تام!
===========================================================================
```

### ج. التحقق الميداني الحي من واجهات الخادم (Live API Verification):
تم فحص الاستجابات الحية من خادم التطبيق النشط على المنفذ `5000`، وجاءت جميع البيانات المرحلة متاحة ومتطابقة تماماً:
* `getCustomers`        : 6 سجلات (العملاء المرحلون + العميل العام).
* `getOrders`           : 5 طلبات بمجموع مبيعات `471,700 YER`.
* `getProducts`         : 5 موديلات فساتين جاهزة.
* `getInventory`        : 7 أصناف أقمشة ومستلزمات خياطة.
* `getPurchases`        : 3 فواتير مشتريات خامات.
* `getVouchers`         : 5 سندات مالية (قبض وصرف).
* `getJournalEntries`   : 4 قيود محاسبية مزدوجة مرحلة.
* **اختبار التكرار (Idempotency Test):** تم إعادة تشغيل السكربت ولم يتكرر أي سجل، مما يضمن أمان التشغيل في أي وقت.

تم بحمد الله وتوفيقه بناء وإتقان ملف المخطط المعماري الكامل لقاعدة بيانات **PostgreSQL** (`schema_postgresql.sql`) متضمناً **51 جدولاً علائقياً كاملاً** يغطي كافة أوراق قوقل شيتس الـ 49 وجداول المنظومة بالكامل:
- **51 جدولاً علائقياً مطبعاً بالكامل (3NF)** مع المفاتيح الأساسية (`PK`) والمفاتيح الأجنبية (`FK`) والقيود (`CHECK`).
- **تغطية شاملة لكافة المنظومات:**
  1. النواة الأساسية، العملات، والمستخدمين (4 جداول).
  2. العملاء، الأطفال، والمقاسات التفصيلية للخياطة (3 جداول).
  3. المنتجات، الطلبات، بنود البيع، والتصنيع (4 جداول).
  4. الموردون، المخزون، المشتريات، وحركات المستودعات (5 جداول).
  5. دليل الحسابات، القيود اليومية، القيد المزدوج، والسندات (5 جداول).
  6. الموارد البشرية ومسير الرواتب (جدولان).
  7. منظومة إدارة وضمان الجودة الشاملة (8 جداول).
  8. ملف المؤسسة، الإعدادات، والفروع والمستأجرين (4 جداول).
  9. منصات التسويق، الشبكات الاجتماعية، والأحداث (3 جداول).
  10. الحملات الإعلانية، المحتوى، والمحادثات الذكية (7 جداول).
  11. ذكاء التسويق، معالجة اللغات واللهجة اليمنية، ونماذج الإسناد (6 جداول).
- **فهارس الأداء العالية (B-Tree Indexes)** على كافة المفاتيح الأجنبية، أرقام الهواتف، والباركود، وتواريخ الحركات.
- **دوال المحرك المحاسبي والترقيم الذري (PL/pgSQL Functions):**
  1. `fn_set_updated_at()`: للتحديث التلقائي اللحظي لحقل `updated_at`.
  2. `fn_generate_next_code(p_key)`: توليد تسلسلات المعرفات الذرية (`CUST-000001`, `ORD-000001`, `PAY-000001`).
  3. `fn_convert_to_base(p_amount, p_currency, p_rate)`: حساب المعادل بالريال اليمني بدقة `NUMERIC(18, 4)`.
  4. `fn_update_account_balances()`: تحديث أرصدة شجرة الحسابات آلياً عند ترحيل القيود اليومية.
  5. `fn_update_inventory_stock()`: تحديث كميات المخزون اللحظية عند إضافة الحركات المستودعية.
  6. `fn_audit_logger()`: تسجيل مسار التدقيق الرقابي التاريخي لكافة العمليات في جدول `audit_logs` بصيغة `JSONB`.
- **المحفزات التلقائية (Triggers):** ربط الدوال بالجداول لتعمل تلقائياً عند أي عملية `INSERT` أو `UPDATE` أو `DELETE`.
- **العروض المحاسبية التنفيذية (Views):**
  1. `v_trial_balance`: ميزان المراجعة اللحظي بالمجاميع والأرصدة.
  2. `v_general_ledger`: دفتر الأستاذ العام الشامل لجميع القيود.
  3. `v_inventory_status`: كشف المخزون المتاح وتنبيهات وصول المواد للحد الأدنى.
  4. `v_orders_detailed`: تقرير الطلبات الشامل مع بيانات العملاء والأطفال والمبالغ المسددة والمتبقية.
- **البيانات التأسيسية الأولية (Seed Data):** إدراج العملات الثلاث (`YER`, `SAR`, `USD`)، تسلسلات الترقيم لـ 22 كياناً، وشجرة الحسابات القياسية المؤسسية.

---

## 11. 🧼 تصفير دليل الحسابات وتوليد الأكواد الآلي (Chart of Accounts Reset & Smart Code Generation)

### أ. تشخيص وحل بقاء الأرصدة بعد التصفير:
1. **السبب الجذري:**
   - كانت دالة التصفير تحذف القيود اليومية لكنها لم تقم بتحديث أعمدة `opening_balance` و `current_balance` في جدول `chart_of_accounts` إلى `0.0`.
   - مسار `/api/accounts/clean-reset` في السيرفر كان يحاول الكتابة في SQLite القديمة والتي كانت تعاني من قفل (`database is locked`).
   - ملف `src/services/api.js` كان يحتوي على شرط fallback يعيد جلب القيود من Google Sheets إذا كانت الاستجابة فارغة.
2. **الإجراءات المنفذة والحل:**
   - تحديث دالة `reset_clean_chart_of_accounts()` في [`pg_service.py`](file:///c:/Users/Mohammed%20Falah/Little_Princesses_ERP/pg_service.py) لتصفير الأرصدة في PostgreSQL مباشرة وحذف القيود والسندات التجريبية.
   - تحويل مسار `/api/accounts/clean-reset` في [`unified_server.py`](file:///c:/Users/Mohammed%20Falah/Little_Princesses_ERP/unified_server.py) ليعتمد بنسبة 100% على PostgreSQL.
   - إزالة الـ fallback القديم لجداول Google Sheets في `api.js` و `Accounts.jsx`.
   - تنفيذ التصفير الفعلي والتحقق من أن جميع الحسابات (28 حساباً) رصيدها `0.00 YER ﷼` والقيود صفرية.

### ب. خوارزمية التوليد الآلي لأكواد الحسابات (Smart Code Generation):
- تمت ترقية التوليد الآلي لأكواد الحسابات في الواجهة والخادم:
  - الحسابات الرئيسية من خانة واحدة (1، 2، 3، 4، 5) تولد تسلسلاً مئوياً (101, 102, 107.. / 201, 202, 203.. / 501, 507..).
  - الحسابات الفرعية والمساعدة (101، 102) تولد تسلسلاً نقطياً ثنائياً (101.01, 101.02, 102.01, 102.02..).
  - فحص مسبق ضد أي تكرار أو تصادم في الأكواد.

---

## 12. 🧾 معالجة سندات القبض ورأس المال وإصلاح القيد المزدوج المتوازن (Vouchers & Equity Accounting Fix)

### أ. تشخيص أسباب الخلل المالي السابق:
1. **تجاهل الحساب المقابل المختار:** كانت دالة `add_voucher` في `pg_service.py` تجبر أي سند قبض على أن يكون الطرف الدائن هو `ACC-104` (ذمم العملاء) تلقائياً، وتتجاهل الحساب الذي حدده المستخدم (`301.02`).
2. **ازدواجية توليد القيود وتضاعف المبلغ:** كان السند ينشئ قيداً أولاً في الخادم برقم `JV-RV-...` ثم ترسل الواجهة قيداً ثانياً برقم `AUTO-VCH-RV-...` مما أدى لتسجيل الحركة مرتين على الصندوق ($795,200 \times 2 = 1,590,400$).
3. **فشل مطابقة أسماء الحسابات المركبة والسقوط في حساب الإيرادات (401):** دالة `resolve_account_id` لم تكن تفصل الكود من النص المركب `"301.02 - راس مال هنادي"`، فسقطت على الحساب الافتراضي `ACC-401` (إيرادات تفصيل الفساتين).
4. **شارة مدين ورصيد سالب لحسابات الإيرادات والخصوم وحقوق الملكية:** في `Accounts.jsx`، كانت المقارنة النصية لنوع الحساب حساسة لحالة الأحرف (`Revenue` vs `REVENUE`)، فتم تصنيف جميع الحسابات خطأً كـ `debit`.

### ب. الإصلاحات المنفذة والمحققة:
1. **ترقية دالة `resolve_account_id`:** لاستخراج الأكواد بدقة من أي نص مركب (مثل `301.02 - ...`) والبحث بالكود والاسم النظيف، وضمان التوجيه لحساب الشريك مباشرة.
2. **ربط سند القبض بالحساب المقابل الفعلي:** في `pg_service.add_voucher`، تم اعتماد `target_acc` لتحديد الطرف الدائن الصحيح، وإضافة عمودي `party_name` و `target_account_id` لجدول `payments`.
3. **توحيد معرف القيد ومنع التكرار:** اعتماد معرف قيد موحد `AUTO-VCH-{voucher_no}` مع تحديث أسطر القيد المزدوج في `journal_entry_lines` لضمان قيد واحد فقط لكل سند.
4. **تصحيح طبيعة الحسابات (`nature`) في الواجهة:** فحص غير حساس لحالة الأحرف مع اعتماد الأكواد القياسية (2، 3، 4 كحسابات دائنة Credit)، مما يعرض حقوق الملكية والإيرادات برصيد موجب وشارة `دائن`.
5. **التحقق التجريبي التام:** تم اختبار قيد إيداع 5,600 ريال سعودي (795,200 ريال يمني) وأثبت النظام:
   - الصندوق (101): `+5,600 SAR` (`795,200 YER`) [مدين].
   - رأس مال هنادي (301.02): `+5,600 SAR` (`795,200 YER`) [دائن].
   - ذمم العملاء (104) والإيرادات (401): بقيت نظيفة عند `0.00 YER ﷼`.

---

## 13. 👑 مزامنة لوحة التحكم التنفيذية (Executive Dashboard) مع البيانات الحية والمحركات المحاسبية

تمت المزامنة الكاملة لملف [`src/features/Dashboard.jsx`](file:///c:/Users/Mohammed%20Falah/Little_Princesses_ERP/src/features/Dashboard.jsx) ليعمل بديناميكية 100% مستنداً إلى سجلات قاعدة البيانات الحية (Supabase / PostgreSQL) والمحركات المحاسبية المتزنة، مع إزالة كافة القيم الثابتة (Hardcoded Values).

### أ. المؤشرات القيادية العلوية (Executive KPI Metric Cards):
1. **إجمالي المبيعات (Total Sales):**
   - الربط المباشر مع مبيعات الطلبيات الحقيقية (`ordersSales = 471,700 YER ﷼`) مع إمكانية القراءة من حسابات الإيرادات (401 / 403) بدلاً من ظهور `0 YER`.
2. **صافي الأرباح المحققة (Net Profit):**
   - إلغاء المعادلة الثابتة `totalSales * 0.35` ونسبة `~35%` التقديرية.
   - احتساب صافي الربح الحقيقي: `صافي الربح = إجمالي المبيعات - إجمالي المصروفات التشغيلية والإنتاجية` (`465,210 YER ﷼`).
   - عرض هامش الربح التشغيلي الديناميكي الفعلي: `~98.6%`.
3. **إجمالي الطلبيات (Total Orders):**
   - عكس عدد طلبات الفساتين الـ 5 المسجلة في النظام مع متوسط قيمة الطلب الحقيقي (`94,340 YER ﷼`).
   - ربط البطاقة بالتنقل الفوري لشاشة الطلبيات (`setActiveTab('orders')`).
4. **أوامر المعمل النشطة (Active Atelier Production):**
   - قراءة وتصنيف فساتين الورشة وفق الكلمات المفتاحية باللغتين العربية والإنجليزية:
     - القص والتجهيز (Cutting): فستان واحد (سارة الكبسي).
     - الخياطة والتجميع (Sewing): فستانان (أميرة الأهدل، مريم العرشي).
     - التطريز والشك (Embroidery): فستان واحد (فاطمة المحضار).
     - الجاهز للتسليم (Ready): فستان واحد (ياسمين الضبيبي).
     - الإجمالي النشط: 5 فساتين.
5. **المستحقات المتبقية (Accounts Receivable):**
   - الربط مع الذمم المتبقية من واقع الطلبيات (`196,100 YER ﷼`) المعلقة للتحصيل عند البروفة والتسليم.
6. **رصيد الخزينة والبنوك (Treasury & Bank Vaults):**
   - قراءة مطابقة 100% لأرصدة الصناديق النقدية بالريال اليمني، الريال السعودي، والدولار (`896,260 YER ﷼`) مع إمكانية النقر للانتقال المباشر لشجرة الحسابات.

### ب. منحنى الإيرادات والمبيعات التفاعلي (Sales Revenue Curve):
- إلغاء حصر التواريخ بـ 14 يوماً السابقة لليوم، والتي كانت تُظهر منحنياً صفرياً مستوياً لكون الطلبيات مؤرخة في يوليو.
- بناء الجدول الزمني التلقائي الممتد من أول عملية بيع حتى آخر طلبية، مما يعرض صعود وهبوط المبيعات اليومية، والتراكمي الفعلي للمبيعات مع نقاط تفاعلية وأدوات توضيحية (Tooltips).

### ج. رادار الخزينة والسيولة النقدية (Treasury Radar):
- احتساب التدفقات الداخلة (Inflow) والخارجة (Outflow) استناداً إلى الحركات المدينة والدائنة لحسابات الصناديق والبنوك، مع عرض صافي التدفق النقدي للفترة بدقة.

### د. مواعيد التسليم القادمة بالورشة (خلال 72 ساعة):
- جلب الطلبيات النشطة غير المسلمة مرتبة تصاعدياً حسب تاريخ التسليم (`delivery_date`).
- إظهار شارات الحالة المعملية الأنيقة المرمزة بالألوان والإيموجي.
- إمكانية النقر المباشر على أي صف للانتقال فوراً لملف الطلب في شاشة المبيعات.

---

## 14. 🧾 ربط فواتير المشتريات والمصروفات بـ Supabase والمحرك المحاسبي المتزن (Purchases & Expenses Live Engine Integration)

تم بنجاح حل ومعالجة كافة المسائل المتعلقة بفواتير شراء الأقمشة والخامات والمصروفات التشغيلية وربطها المباشر بقاعدة بيانات Supabase / PostgreSQL والمحرك المحاسبي المزدوج:

### أ. ما تم تصحيحه وضبطه برمجياً ومحاسبياً:
1. **ربط مسار المشتريات بالخادم الموحد (`POST /api/purchases`):**
   - تم استدعاء `pg_service.add_purchase()` مباشرة عند حفظ أو تحديث أي فاتورة مشتريات.
   - **تسجيل المورد كاملاً:** يتم إدراج المورد وبياناته وهاتفه تلقائياً في جدول `suppliers` في Supabase إن لم يكن موجوداً، أو تحديث هاتفه إن وجد.
   - **حفظ المرفقات والصور:** يتم حفظ رابط صورة الفاتورة (`invoice_attachment`) ورابط صورة السند (`receipt_attachment`) في قاعدة البيانات السحابية لتظهر فوراً في جدول الفواتير وزر المعاينة.
2. **محرك توزيع القيود اليومية المحاسبية المزدوجة (5 أسطر متزنة):**
   - **مدين مخزون الأقمشة والخامات (`ACC-105`):** بإجمالي قيمة الأصناف المشتراة.
   - **مدين مصاريف الشحن والنقل (`ACC-507`):** بتكلفة الشحن المدخلة.
   - **مدين رسوم وعمولات التحويل (`ACC-508`):** برسوم الحوالة المدخلة.
   - **دائن الخصم والتخفيض التجاري المكتسب (`ACC-403`):** بقيمة الخصم الممنوح من المورد.
   - **دائن حساب السداد المخصص:** يوجه آلياً إلى صندوق الريال السعودي (`ACC-101-2`) أو صندوق الدولار (`ACC-101-3`) أو الصندوق الرئيسي (`ACC-101`) أو ذمم الموردين (`ACC-201` عند الآجل).
3. **توجيه الصناديق التلقائي في واجهة المشتريات (`Purchases.jsx`):**
   - عند اختيار العملة **SAR**، يتم تحويل حساب الدفع تلقائياً إلى `101.2 - صندوق الريال السعودي (SAR)`.
   - عند اختيار العملة **USD**، يتم التحويل تلقائياً إلى `101.3 - صندوق الدولار (USD)`.
   - عند اختيار طريقة الدفع **آجل**، يتم التحويل تلقائياً إلى `201 - ذمم الموردين ومحلات الأقمشة`.
4. **ربط مسار المصروفات التشغيلية (`POST /api/expenses/create`):**
   - استدعاء `pg_service.add_expense()` لتسجيل المصروف وقيده المزدوج وسند الصرف مباشرة في Supabase.

### ب. نتائج التحقق التجريبي الحي (Empirical Test Results):
- تم تسجيل فاتورة تجريبية برقم `TEST-PUR-9001` بالريال السعودي مع خصم 50 ريال، وشحن 20 ريال، ورسوم تحويل 10 ريالات، وإرفاق الصور:
  - تم إنشاء المورد في `suppliers`: `مؤسسة النجوم للأقمشة الفاخرة` (هاتف: 771234567).
  - تم توجيه السند المالي `PAY-TEST-PUR-9001` إلى صندوق الريال السعودي (`ACC-101-2`) بمبلغ 980 ريال سعودي.
  - تم توليد قيد اليومية المزدوج `JV-PUR-TEST-PUR-9001` متزناً بنسبة 100%:
    $$\sum \text{Debit Base} = 146,775.00 \text{ YER} = \sum \text{Credit Base} = 146,775.00 \text{ YER}$$
    $$\text{Diff} = 0.00 \text{ YER}$$
  - بعد نجاح الاختبار، تم تنظيف وحذف السجل التجريبي للحفاظ على نظافة ودقة النظام.

---

## 15. 🧵 إدارة المشتريات، خامات الأقمشة والمخزون، الموردين، وإهلاك التالف المحاسبي (Purchases, BOM Inventory & Suppliers Full Engine)

تم بنجاح استكمال وربط منظومة **المشتريات، خامات ومخزون الأقمشة (BOM)، الموردين، والتسويات الجردية المزدوجة** بشكل كامل ومباشر مع قاعدة بيانات Supabase / PostgreSQL السحابية، وضمان عدم وجود سجلات أيتام أو أخطاء تكرار:

### أ. ما تم إنجازه وضبطه في المعمارية البرمجية والمحاسبية:
1. **دعم الفواتير متعددة الأصناف (Multi-Item Purchase Invoices):**
   - ترقية جدول `purchases` وحفظ الأصناف التفصيلية في جدول `purchase_items` المرتبط بنقاء وتوافق تام.
   - حساب تلقائي لمتوسط التكلفة المرجح (Weighted Average Cost) لكل خامة وقماش يدخل المخزون:
     $$\text{New Cost} = \frac{(\text{Old Qty} \times \text{Old Cost}) + (\text{New Qty} \times \text{New Price})}{\text{Old Qty} + \text{New Qty}}$$
2. **إدارة الموردين المتكاملة (Suppliers Management):**
   - إضافة نقاط نهاية REST مركزية: `GET /api/suppliers` و `POST /api/suppliers` و `POST /api/suppliers/delete`.
   - الربط الكامل مع مسارات `callGAS` لضمان عمل الواجهة بسلاسة في كلا الوضعين.
   - إنشاء الموردين الجدد تلقائياً في جدول `suppliers` وتحديث أرقام هواتفهم وأرصدتهم.
3. **التسويات الجردية وإهلاك التالف المحاسبي (Inventory Wastage & Gain):**
   - تأسيس حسابات شجرة الحسابات السحابية: `ACC-509` (هالك وتالف الأقمشة والخامات - مصروفات) و `ACC-404` (أرباح وتسويات جرد المخزون - إيرادات).
   - مسار مخصص `POST /api/inventory/adjust` يخصم أو يضيف الكميات من جدول `inventory`، ويسجل حركة المخزون في `inventory_transactions`.
   - توليد قيد يومية مزدوج متوازن تلقائياً:
     - عند الهالك والتالف: مدين `ACC-509` (هالك وتالف الأقمشة) / دائن `ACC-105` (مخزون الأقمشة والخامات).
     - عند الفائض الجردي: مدين `ACC-105` (مخزون الأقمشة) / دائن `ACC-404` (أرباح تسوية جردية).
4. **معالجة القيود والحقول المحسوبة سحابياً (Generated Always Stored Columns):**
   - ضبط الاستعلامات للتعامل السليم مع الأعمدة المحسوبة تلقائياً في PostgreSQL (`available_qty`, `total_value`, `total_cost`).
5. **سياسة الحذف النظيف الشامل (Zero Orphan Records):**
   - عند حذف أي فاتورة مشتريات عبر `delete_purchase`، يتم الحذف التسلسلي الذري لأسطر القيد `journal_entry_lines`، وقيود اليومية `journal_entries`، وسندات الصرف `payments`، وحركات المخزون `inventory_transactions`، وأصناف الفاتورة `purchase_items`، وسجل الشراء نفسه.

### ب. نتائج التحقق والاختبار الفعلي الحي (End-to-End Verification):
تم تنفيذ سكريبت التحقق الشامل [`verify_purchases_inventory_full.py`](file:///C:/Users/Mohammed%20Falah/.gemini/antigravity/brain/66c8cea0-1166-4bc1-afc7-092086332a03/scratch/verify_purchases_inventory_full.py) عبر خادم النظام الحقيقي، واجتاز المراحل الخمس بنجاح بنسبة 100%:
- **المرحلة 1:** فحص نقاط جلب المخزون والموردين والمشتريات (`GET /api/inventory/items`, `/api/suppliers`, `/api/purchases`) بنجاح (200 OK).
- **المرحلة 2:** إنشاء مورد جديد عبر REST والتحقق من ظهوره الفوري في القوائم.
- **المرحلة 3:** إنشاء فاتورة مشتريات متعددة الأصناف (حرير تويل + شيفون إيطالي) مع خصم وشحن ورسوم حوالة وتوليد سند الصرف والقيد المزدوج المتوازن والتحقق من زيادة أرصدة المخزون بالكميات ومتوسطات التكلفة المرجحة.
- **المرحلة 4:** تنفيذ تسوية جردية لإهلاك تالف قص فستان (خصم 2.5 متر) وتوليد قيد الإهلاك المحاسبي بنجاح تام.
- **المرحلة 5:** حذف الفاتورة والمورد التجريبي وتنظيف كافة السجلات المؤقتة بنجاح دون أي بقايا.
```text
Starting End-to-End Verification of Purchases, Inventory & Suppliers...
--- 1. Testing GET Endpoints ---
[PASS] GET /api/inventory/items: OK (11 items)
[PASS] GET /api/suppliers: OK (7 suppliers)
[PASS] GET /api/purchases: OK (6 purchases)
--- 2. Testing Supplier CRUD ---
[PASS] Supplier Created: ID=SUPP-8991575-E87D
[PASS] Supplier Verified in GET /api/suppliers
--- 3. Testing Purchase Creation with Inventory Link & Accounting ---
[PASS] Purchase Created via REST: PUR-TEST-1788991577
[PASS] Inventory verified: حرير تويل تجريبي (qty=20.0, cost=1500.0), شيفون إيطالي تجريبي (qty=10.0, cost=2500.0)
--- 4. Testing Inventory Adjustment (Wastage & Double-Entry) ---
[PASS] Wastage Adjustment Executed: new_qty=17.5 (decreased by 2.5m)
[PASS] Inventory record verified in database: current qty = 17.5m
--- 5. Cleanup Test Records ---
[PASS] Test Purchase Deleted: PUR-TEST-1788991577
[PASS] Test Supplier Cleaned Up: SUPP-8991575-E87D
[PASS] Test Inventory and Journal Entries Cleaned Up (Zero Residuals)
🎉 ALL TESTS PASSED SUCCESSFULLY! 100% OPERATIONAL.
```

---

## 👗 المرحلة 19: حاسبة تكاليف الموديلات ومصفوفة استهلاك الأقمشة والكتالوج الملكي (Dress Costing, Fabric BOM & Haute Couture Catalog)

تم بحمد الله وتوفيقه إنجاز وتشغيل محرك مقايسة وتكاليف الموديلات والكتالوج الملكي لفساتين الأطفال الراقية بكفاءة 100%، وربطه بقاعدة بيانات PostgreSQL 17 Cloud وشاشات النظام:

### 1. هيكلة قاعدة البيانات السحابية (PostgreSQL 17.6 Cloud):
- تمت ترقية جدول `products` وإضافة أعمدة دعم المقايسات المتقدمة وقائمة المواد:
  - `bom` (`JSONB`): قائمة مواد الفستان ومصفوفة استهلاك الأقمشة (اسم القماش، تكلفة المتر، والأمتار المطلوبة للفئات العمرية الأربع: `1-2 سنة`, `3-5 سنوات`, `6-9 سنوات`, `10-13 سنة`).
  - `price_matrix` (`JSONB`): مصفوفة أسعار البيع المعتمدة لكل فئة عمرية.
  - `labor_cost` (`NUMERIC`): تكلفة الخياطة والتطريز والشك المباشرة للقطعة.
  - `packaging_cost` (`NUMERIC`): تكلفة التغليف الملكي والملصقات والأكياس الفاخرة.
  - `fabric_cost` (`NUMERIC`): إجمالي تكلفة الأقمشة المحسوبة للقطعة.
  - `age_chart` (`JSONB`): جدول الأعمار والمقاسات القياسية المعتمدة للموديل.

### 2. خدمة الباك إند وتكامل الخادم الموحد (`pg_service.py` & `unified_server.py`):
- **ترقية `get_products`:** جلب الموديلات مع تفكيك حقول `JSONB`، واستخلاص ملخص خامات الفستان (`fabric_name`) ومتوسط الأمتار المستهلكة (`yards_used`)، واحتساب هامش الربح المتوقع (`profit = sell_price - total_cost`).
- **ترقية `add_product` و `save_bom_model`:** حفظ وتوثيق الموديل الجديد أو المحدث، وتخزين مصفوفة الـ BOM كاملة وحساب سعر البيع التلقائي عند تحديد الفئات العمرية.
- **دالة `get_bom_models`:** تزويد الواجهة بالموديلات بالإضافة إلى مؤشرات الأداء الحسابية السريعة (إجمالي الموديلات، متوسط التكلفة، متوسط سعر البيع، ومتوسط هامش الربح `avg_margin %`).
- **ترقية `delete_product`:** حذف آمن وغير هدام مع التحقق من عدم وجود طلبيات مبيعات سابقة مرتبطة بالموديل، وأرشفته بدلاً من كسره إن وجدت طلبيات.
- **مسارات REST المباشرة:**
  - `GET /api/products/bom`
  - `POST /api/products/bom`
  - `POST /api/products/delete`
  - `GET & POST /api/products`

### 3. الربط الذكي والتلقائي مع شاشة المبيعات والطلبيات (`Orders.jsx`):
- عند اختيار اسم الموديل واسم الطفلة في فاتورة المبيعات:
  - يستنتج النظام الفئة العمرية للطفلة من جدول القياسات (`child.estimated_age`).
  - يبحث آلياً في مصفوفة أسعار الموديل (`p.price_matrix[child.estimated_age]`).
  - يقوم بإسقاط سعر البيع المعتمد مباشرة في حقل إجمالي الفاتورة دون أي تدخل يدوي!

### 4. الفحص والتحقق التجريبي الفعلي (Empirical Runtime Verification):
- تم تنفيذ سكربت الفحص الشامل `scratch/verify_products_bom_full.py` واجتيازه بنجاح 100%:
  - ✅ حفظ موديل فاخر عبر `REST POST /api/products/bom` بمصفوفة قماشين (تول إيطالي + ساتان ملكي) وتكاليف تشغيل وتغليف.
  - ✅ فحص صحة ومطابقة أعمدة `JSONB` في سحابة PostgreSQL مباشرة.
  - ✅ استرجاع الموديل عبر `REST GET /api/products/bom` والتحقق من حسابات الربح وملخص الأقمشة ومؤشرات الأداء.
  - ✅ فحص استجابة فعل `callGAS("getProducts")` وتوافقه مع واجهة التطبيق.
  - ✅ تحديث مصفوفة أسعار الموديل والتأكد من انعكاسها الفوري سحابياً.
  - ✅ حذف الموديل عبر `REST POST /api/products/delete` والتأكد من التطهير الكامل بقاعدة البيانات (0 سجلات متبقية).

```text
======================================================================
🧪 STARTING EMPIRICAL VERIFICATION: PRODUCTS, BOM & COSTING CALCULATOR
======================================================================
1.1 REST POST /api/products/bom: True | ID: PROD-TEST-ROYAL-01 | Msg: تم حفظ وتوثيق الموديل (فستان لؤلؤة المملكة الملكي - اختبار) وحساب التكلفة سحابياً ☁️🧮
2.1 Direct DB Verification: Columns, JSONB BOM, and Price Matrix verified 100% in Cloud.
3.1 REST GET /api/products/bom: True | Models Count: 6 | KPIs: {'total_models': 6, 'avg_cost': 59951.67, 'avg_price': 75120.0, 'avg_margin': 20.2}
3.2 Verified Model Data: Name=فستان لؤلؤة المملكة الملكي - اختبار, Fabric Summary='تول إيطالي فاخر (2.0م متوسط) + ساتان ملكي كوري (1.5م متوسط)', Profit=115.0
4.1 GAS Action getProducts: True | Count: 6
5.1 REST POST /api/products/bom (Update): True
5.2 Updated Price Matrix verified in PostgreSQL Cloud.
6.1 REST POST /api/products/delete: True | Deleted: True | Msg: تم حذف الموديل بنجاح 🗑️
7.1 Zero Residual Test Data verified: Database completely clean.
======================================================================
🎉 ALL PRODUCTS & BOM INTEGRATION TESTS PASSED 100% WITH ZERO ERRORS!
======================================================================
```

---

## ⚙️ المرحلة 22: إعدادات النظام العامة وسجلات تدقيق الأمان والنسخ الاحتياطي السحابي (System Settings, Security Audit Logs & Cloud Backups)

تم بحمد الله وتوفيقه استكمال **المرحلة 22** بنجاح باهر وبأعلى معايير الأمان والرقابة والموثوقية، مع ربطها مباشرة بقاعدة بيانات **PostgreSQL 17 السحابية (Cloud Pool)**:

### 1. إعدادات المؤسسة والمظهر والعملات المعتمدة (Company Profile & FX Rates):
- **الربط السحابي المباشر:**
  - استرجاع وحفظ بيانات المؤسسة (الاسم، الهاتف، المقر، البريد، بداية السنة المالية، والمظهر المعتمد) مباشرة في جدول `company_profile`.
  - تحديث وحفظ أسعار صرف العملات الحية (الريال السعودي والدولار الأمريكي مقابل الريال اليمني YER) في جدول `currencies` مع توثيق وقت التعديل `last_updated`.
  - تخزين واسترجاع معلمات النظام العامة من جدول `system_settings`.
- **محرك المظهر اللحظي (Global Theme Engine):**
  - دعم التبديل السلس بين النمط الفاتح (Light Theme) والنمط الليلي الكحلي عالي التباين (Dark Theme) وحفظ تفضيل المستخدم سحابياً.

### 2. استوديو سجلات تدقيق الأمان والعمليات (Security Audit Logs Studio):
- **الرصد والتتبع الآلي (Automated Real-Time Auditing):**
  - توثيق كافة التعديلات، الحركات المالية، وحفظ الإعدادات آلياً في جدول `audit_logs` بقاعدة البيانات السحابية مع حل معرف المستخدم (`user_id`) بدقة.
  - دعم تسجيل الأحداث الميدانية (تسجيل الدخول، إنشاء النسخ الاحتياطية، الاستعادة، والعمليات الحساسة) مع تسجيل عنوان الـ IP والوقت والتاريخ.
- **واجهة مراقبة تفاعلية متطورة في `Settings.jsx`:**
  - شريط بحث وفلترة فورية حسب نوع الإجراء (`CREATE`, `UPDATE`, `DELETE`, `SNAPSHOT`, `RESTORE`, `LOGIN`) ونوع الكيان (`SETTINGS`, `ORDERS`, `EXPENSES`, `BACKUP`, `HR`, `JOURNAL`).
  - جدول تفاعلي يعرض التوقيت، المستخدم، عنوان IP، شارة نوع الإجراء الملونة، والكيان المستهدف.
  - نافذة منبثقة تفاعلية (Modal Inspector) لمعاينة تفاصيل التغييرات ومقارنة القيم السابقة (`old_values`) بالقيم الجديدة (`new_values`).

### 3. محرك النسخ الاحتياطي السحابي واستعادة البيانات والكوارث (Backup & Disaster Recovery Suite):
- **مؤشرات حية لقاعدة البيانات السحابية:**
  - قراءة الحجم الحقيقي لقاعدة البيانات السحابية في PostgreSQL (`pg_database_size`) وعرضه بتنسيق مقروء (مثل `14.36 MB`).
  - حساب إجمالي السجلات والبيانات المحمية لحظياً عبر كافة جداول النظام الـ 23.
  - فحص سلامة وتطابق الجداول (`PASSED / 100% OK`).
- **إنشاء نقاط الاستعادة الفورية (Cloud Snapshots):**
  - تصدير لقطة سحابية شاملة لكافة الجداول التشغيلية والمحاسبية بصيغة JSON منظمة وتخزينها في مجلد `backups/` مع قيد العملية آلياً في سجل التدقيق الأمني.
- **تصدير واستعادة البيانات:**
  - مسار تنزيل وتصدير مباشر عبر REST (`GET /api/backup/export?format=json`).
  - محرك استعادة بيانات آمن يعيد بناء الجداول وسجلاتها بالترتيب الدقيق للمفاتيح الأجنبية.
  - جدول استعراض لكافة النسخ ونقاط الاستعادة السابقة المحفوظة على الخادم مع أحجامها وتواريخها.

### 4. واجهة المستخدم المبوّبة (`src/features/Settings.jsx`):
- تقسيم الشاشة إلى 3 تبويبات أنيقة وسريعة التبديل (Tabs):
  1. `⚙️ إعدادات المؤسسة والمظهر والعملات`
  2. `🛡️ سجلات تدقيق الأمان والعمليات (Audit Logs)`
  3. `💾 النسخ الاحتياطي واستعادة البيانات (Backup & DR)`

### 5. التحقق والتشغيل الفعلي (Empirical Runtime Verification):
- تشغيل سكربت الفحص الشامل `scratch/verify_settings_audit_backup_full.py` واجتيازه بنجاح 100%:
  - ✅ استجابة مسارات GET للإعدادات وأسعار الصرف.
  - ✅ تحديث إعدادات الشركة وأسعار الصرف في `company_profile` و `currencies`.
  - ✅ التحقق من الرصد الآلي للحدث في `audit_logs` وتسجيل حدث عبر REST وفلترة السجلات.
  - ✅ استعلام حالة قاعدة البيانات السحابية ومؤشرات الحجم والسجلات المحمية.
  - ✅ إنشاء لقطة سحابية فورية وحفظ ملف الـ JSON شاملاً لـ 23 جدولاً.
  - ✅ فحص مسار تصدير النسخة وتنزيلها 200 OK.
  - ✅ استعادة البيانات الأصلية وتطهير البيانات التجريبية وملف اللقطة بنسبة 100% (Zero Residual Data).
- فحص الانحدار لكافة وحدات النظام الـ 20 عبر كل من مسارات REST و جسر GAS (`verify_all_modules_connectivity.py`) واجتيازه بنجاح 100%.

---

## 23. ⚡ واجهة الكاشير ونقاط البيع السريعة والباركود والطباعة الحرارية (POS Touch & Barcode Register Studio)

تم بنجاح تحويل وحدة المبيعات في دار **Little Princesses ERP** إلى استوديو نقاط بيع وكاشير فائق السرعة مزدوج النمط (Dual-Mode Studio)، يربط صالات العرض والمعارض اللمسية وقارئ الباركود مباشرة بقاعدة بيانات **PostgreSQL 17 Cloud** ومحرك الفواتير والطباعة الحرارية 80mm:

### 1. نظام التبديل المزدوج في الشاشة (Dual-Mode Studio):
- **تبويب كاشير ونقاط البيع السريعة (POS Touch & Barcode Register):** واجهة مخصصة لصالات البيع المباشر والكاشير، تدعم شاشات اللمس والماسح الضوئي للباركود والسلة السريعة متعددة الأصناف.
- **تبويب سجل وأرشيف الفواتير والطلبات (Orders Ledger & Archive):** الاحتفاظ بكامل وظائف حجز فساتين الأطفال التفصيلية، المقاسات، عروض الأسعار، إرسال واتساب، وتعديل ومتابعة مراحل الإنتاج، مع الحفاظ التام 100% على كافة البيانات السابقة.

### 2. كتالوج الأصناف اللمسي وقارئ الباركود (Touch Catalog & Barcode Gun Scanner):
- **حقل قارئ الباركود المتطور (Barcode Scanner Gun Input):**
  - استماع مباشر للماسح الضوئي للباركود أو الإدخال اليدوي لرمز الـ SKU أو الاسم مع الضغط على `Enter`.
  - إضافة فورية للصنف إلى السلة، أو زيادة كميته تلقائياً إذا كان موجوداً مسبقاً، مع إشعار نجاح فوري.
- **شريط فلاتر التصنيفات اللمسية (Category Pills):**
  - فلاتر سريعة للأصناف: `الكل`، `فساتين سهرة`، `فساتين زفاف`، `فساتين كاجوال`، `زي مدرسي`، `تفصيل خاص` مع إظهار عدد الموديلات في كل قسم.
- **شبكة كروت الموديلات اللمسية (Touch Product Cards Grid):**
  - كروت أنيقة متوافقة مع الهوية الملكية، تعرض أيقونة أو صورة الفستان، رمز الـ SKU، التصنيف، اسم الموديل، السعر المعتمد، وشارة الكمية اللحظية الموجودة بالسلة، مع زر إضافة سريع بنقرة واحدة.

### 3. لوحة السلة والمحاسبة الفورية (POS Cart & Cash Change Engine):
- **اختيار العميل والأميرة:**
  - مهيأ افتراضياً لـ `عميل عام / زائر صالة العرض` (`CUST-GENERAL`) لسرعة البيع المباشر، مع قائمة منسدلة لاختيار أي عميل مسجل أو إدخال اسم الطفلة لفساتين التفصيل.
- **جدول بنود السلة التفاعلي:**
  - عرض بنود السلة مع رمز SKU والسعر الإفرادي.
  - أزرار سريعة للتحكم بالكمية (`-` / `+`)، احتساب إجمالي السطر لحظياً، وزر حذف الصنف (`✕`).
  - إمكانية إفراغ السلة بالكامل بنقرة واحدة (`إفراغ السلة 🗑️`).
- **حاسبة الخصم والمجاميع:**
  - احتساب المجموع الفرعي (Subtotal).
  - إدخال مبلغ الخصم الممنوح (Discount).
  - احتساب الإجمالي الصافي النهائي (Net Total).
- **أزرار طرق الدفع السريعة (Payment Tenders):**
  - 💵 **نقد (كاش)**
  - 💳 **شبكة / مدى / بنك**
  - 📑 **آجل / قيد الحساب (Credit / Split)**
- **حاسبة النقدية والصرف اللحظية (Cash Change Engine):**
  - حقل إدخال المبلغ المستلم من العميل مع أزرار سريعة للمبالغ الشائعة (`مضبوط ✅`، `+500`، `+1000`، `+2000`، `+5000`، `+10000`).
  - شريط ذكي ملون يعرض لحظياً:
    - `💵 الصرف المستحق للعميل: X` باللون الأخضر.
    - `⏳ المتبقي كدين على العميل: X` باللون البرتقالي للبيع الآجل.
    - `✅ المبلغ مطابق تماماً` باللون الأزرق.

### 4. إتمام البيع السحابي وتوليد الفاتورة الحرارية 80mm:
- زر كبير مخصص للكاشير: `⚡ إتمام البيع والطباعة الفورية (80mm Receipt)` مع مؤشر تحميل لمنع التكرار.
- استدعاء خلفي مباشر لـ `POST /api/sales/orders` ينفذ ذرياً في معاملة موحدة (Single DB Transaction):
  1. قيد الفاتورة في جدول `orders`.
  2. قيد كافة البنود وتفاصيلها في جدول `order_items`.
  3. الخصم الآلي من المخزون `inventory` وتسجيل حركات الصرف في `inventory_transactions`.
  4. إصدار سند قبض رسمي في `payments` برقم `REC-{order_no}`.
  5. توليد قيد يومية متوازن آلياً في `journal_entries`.
  6. إنشاء أمر تشغيل في `production_orders`.
  7. تسجيل العملية في سجل تدقيق الأمان `audit_logs`.
- فتح نافذة الطباعة الحرارية المتقدمة `PrintModal` تلقائياً فور إتمام البيع، محملة بقالب الفاتورة الحرارية 80mm وجدول الأصناف المتعددة، ملخص المبالغ والخصومات، ورمز الاستجابة السريعة QR Code.

### 5. التحقق والتشغيل الفعلي (Empirical Runtime Verification):
- تشغيل سكربت الفحص الميداني الكامل للمرحلة 23 `scratch/verify_pos_touch_full.py` واجتيازه بنجاح 100%:
  - ✅ التحقق من اتصال الخادم وقاعدة البيانات السحابية PostgreSQL 17 Cloud.
  - ✅ التحقق من العميل العام `CUST-GENERAL` وجلب كتالوج الأصناف المباشرة.
  - ✅ محاكاة تنفيذ طلب مبيعات POS متعدد البنود وتطبيق خصم مالي وإصدار الفاتورة عبر REST بنجاح تام.
  - ✅ التحقق من سلامة قيد الفاتورة في جدول `orders`.
  - ✅ التحقق من حفظ كافة بنود السلة بدقة في جدول `order_items`.
  - ✅ التحقق من توليد سند القبض في جدول `payments`.
  - ✅ التحقق من توليد وترحيل القيد المحاسبي المتوازن في `journal_entries`.
  - ✅ التحقق من مطابقة حمولة الفاتورة الحرارية 80mm بنسبة 100% مع مكون `PrintModal.jsx`.
  - ✅ تنظيف وتطهير كافة البيانات الاختبارية من قاعدة البيانات وضمان صفر بقايا (Zero Residual Data).
- تشغيل فحص الانحدار الشامل لجميع وحدات ومراحل النظام الـ 20 (`scratch/verify_all_modules_connectivity.py`) وتأكيد اتصال وسلامة النظام بالكامل بنسبة 100% نجاح أخضر.

---

## 24. 🧹 التصفير الشامل لكافة بيانات وعمليات النظام (Full System Zero Reset / Clean Slate)

تم بنجاح وبناءً على التوجيه المباشر تنفيذ عملية التصفير الشامل لكافة البيانات والعمليات التشغيلية والتجريبية في **Little Princesses ERP** وإرجاع النظام إلى حالة المصنع النقية (Clean Slate):

### 1. إجراءات الأمان والنسخ الاحتياطي المسبق (Safety Backup Snapshot):
- قبل البدء بالحذف، تم توليد وتصدير لقطة سحابية كاملة للبيانات السابقة في:
  `backups/snapshot_20260910_181842.json`
  لضمان إمكانية الرجوع إليها في أي وقت.

### 2. التصفير الذري الشامل في سحابة PostgreSQL 17 Cloud:
- تصفير وحذف كافة السجلات في الجداول التشغيلية حسب الترتيب الهندسي الدقيق للمفاتيح الأجنبية:
  - `orders` و `order_items`: **0 فواتير**.
  - `payments`: **0 سندات قبض أو صرف**.
  - `production_orders`: **0 أوامر تشغيل بالمعمل**.
  - `inventory` و `inventory_transactions`: **0 أصناف و 0 حركات مخزنية**.
  - `purchases` و `purchase_items`: **0 فواتير مشتريات**.
  - `suppliers`: **0 موردين**.
  - `expenses`: **0 مصاريف تشغيلية**.
  - `journal_entries` و `journal_entry_lines`: **0 قيود يومية**.
  - `products`: **0 موديلات وتصاميم**.
  - `employees` و `payroll`: **0 موظفين و 0 مسيرات رواتب**.
  - `quality_*` (8 جداول للجودة والعيوب والتقييمات): **0 سجلات**.
  - `campaigns`: **0 حملات تسويقية**.
  - `children` و `measurements`: **0 سجلات مقاسات**.
- **الحفاظ على التأسيس الهيكلي والأمني:**
  - `chart_of_accounts`: الإبقاء على هيكل الدليل المحاسبي (35 حساباً) مع تصفير أرصدتها بنسبة 100% (`current_balance = 0.00`, `opening_balance = 0.00`).
  - `currencies`: الإبقاء على العملات الرسمية (YER, SAR, USD) مع أسعار الصرف القياسية.
  - `users`: الحفاظ على حساب المدير العام (`admin`) والصلاحيات المعتمدة لضمان استمرارية تسجيل الدخول.
  - `customers`: إعادة تأسيس العميل العام الافتراضي صالة العرض (`CUST-GENERAL`) برصيد 0.00.
  - `number_sequences`: تصفير كافة العدادات والمتسلسلات لتبدأ من الرقم `1`.
  - `audit_logs`: تصفير السجلات السابقة وقيد حدث التصفير الشامل برمز `FACTORY_RESET`.

### 3. مطابقة وتصفير قواعد بيانات SQLite المحلية:
- مسح وتصفير الجداول المتزامنة في `little_princesses.db` و `database.db`.

### 4. التحقق والتشغيل الفعلي بعد التصفير:
- تم تشغيل فحص الاتصال الشامل `scratch/verify_all_modules_connectivity.py` على كافة أقسام النظام الـ 20 بعد التصفير، وأظهرت النتائج:
  - **100% متصل بنجاح أخضر عبر كافة مسارات REST وجسر GAS**.
  - **كافة الأقسام تبدأ من 0 سجلات وأرصدة دفترية 0.00 YER متزنة تماماً**.


